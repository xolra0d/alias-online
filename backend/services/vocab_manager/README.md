# vocab\_manager

```go
import "github.com/xolra0d/alias-online/services/vocabs"
```

## Index

- [func RunGrpcServer\(vocabManager \*VocabManager, logger \*slog.Logger, runningAddr string, shutdownTimeout time.Duration\)](<#RunGrpcServer>)
- [type Postgres](<#Postgres>)
  - [func NewPostgres\(postgresUrl string, logger \*slog.Logger\) \(\*Postgres, error\)](<#NewPostgres>)
  - [func \(p \*Postgres\) Close\(\)](<#Postgres.Close>)
  - [func \(p \*Postgres\) LoadVocabs\(ctx context.Context\) \(map\[string\]Vocabulary, bool\)](<#Postgres.LoadVocabs>)
- [type ServerConfig](<#ServerConfig>)
  - [func LoadServerConfig\(\) \*ServerConfig](<#LoadServerConfig>)
- [type VocabManager](<#VocabManager>)
  - [func NewVocabManager\(db \*Postgres, logger \*slog.Logger, loadVocabsTimeout, pollInterval, closePostgresConnTimeout time.Duration\) \(\*VocabManager, bool\)](<#NewVocabManager>)
  - [func \(v \*VocabManager\) AvailableVocabs\(\) \[\]string](<#VocabManager.AvailableVocabs>)
  - [func \(v \*VocabManager\) StartObservation\(\)](<#VocabManager.StartObservation>)
  - [func \(v \*VocabManager\) StopObservation\(\)](<#VocabManager.StopObservation>)
  - [func \(v \*VocabManager\) Vocab\(name string\) Vocabulary](<#VocabManager.Vocab>)
- [type Vocabulary](<#Vocabulary>)


<a name="RunGrpcServer"></a>
## func RunGrpcServer

```go
func RunGrpcServer(vocabManager *VocabManager, logger *slog.Logger, runningAddr string, shutdownTimeout time.Duration)
```



<a name="Postgres"></a>
## type Postgres



```go
type Postgres struct {
    // contains filtered or unexported fields
}
```

<a name="NewPostgres"></a>
### func NewPostgres

```go
func NewPostgres(postgresUrl string, logger *slog.Logger) (*Postgres, error)
```

NewPostgres creates new instance of postgres client.

<a name="Postgres.Close"></a>
### func \(\*Postgres\) Close

```go
func (p *Postgres) Close()
```

Close closes postgres pool.

<a name="Postgres.LoadVocabs"></a>
### func \(\*Postgres\) LoadVocabs

```go
func (p *Postgres) LoadVocabs(ctx context.Context) (map[string]Vocabulary, bool)
```

LoadVocabs loads vocabularies from postgres.

<a name="ServerConfig"></a>
## type ServerConfig



```go
type ServerConfig struct {
    // DATABASES
    PostgresUrl              string        // Env name: `POSTGRES_URL`. PostgreSQL connection string. Default: none, will panic, if not set.
    LoadVocabsTimeout        time.Duration // Env name: `LOAD_VOCABS_TIMEOUT`. Max wait time for loading vocab_manager in seconds. Default: 10.
    ClosePostgresConnTimeout time.Duration // Env name: `CLOSE_POSTGRES_CONN_TIMEOUT`. Max wait time for closing postgres connection in seconds. Default: 10.

    // HTTP
    RunningAddr     string        // Env name: `RUNNING_ADDR`. Address to run web on. Default: `:8070`.
    ShutdownTimeout time.Duration // Env name: `SHUTDOWN_TIMEOUT`. Time for transport server to shut down in seconds. Default: 10.

    // Vocabs
    VocabsPollInterval time.Duration // Env name: `VOCABS_POLL_INTERVAL`. Wait time between checks for vocab_manager update in seconds. Default: 10.
}
```

<a name="LoadServerConfig"></a>
### func LoadServerConfig

```go
func LoadServerConfig() *ServerConfig
```



<a name="VocabManager"></a>
## type VocabManager



```go
type VocabManager struct {
    LoadVocabsTimeout        time.Duration
    PollInterval             time.Duration
    ClosePostgresConnTimeout time.Duration
    // contains filtered or unexported fields
}
```

<a name="NewVocabManager"></a>
### func NewVocabManager

```go
func NewVocabManager(db *Postgres, logger *slog.Logger, loadVocabsTimeout, pollInterval, closePostgresConnTimeout time.Duration) (*VocabManager, bool)
```

NewVocabManager creates new vocab manager.

<a name="VocabManager.AvailableVocabs"></a>
### func \(\*VocabManager\) AvailableVocabs

```go
func (v *VocabManager) AvailableVocabs() []string
```

AvailableVocabs returns names of available vocabs.

<a name="VocabManager.StartObservation"></a>
### func \(\*VocabManager\) StartObservation

```go
func (v *VocabManager) StartObservation()
```

StartObservation subscribes to updates from postgres to refresh vocabularies efficiently.

<a name="VocabManager.StopObservation"></a>
### func \(\*VocabManager\) StopObservation

```go
func (v *VocabManager) StopObservation()
```

StopObservation stops current observation loop.

<a name="VocabManager.Vocab"></a>
### func \(\*VocabManager\) Vocab

```go
func (v *VocabManager) Vocab(name string) Vocabulary
```

Vocab returns vocabulary with given name. Will return empty vocab, if this name does not exist.

<a name="Vocabulary"></a>
## type Vocabulary

Vocabulary is vocabulary of words for room.

```go
type Vocabulary struct {
    PrimaryWords []string
    RudeWords    []string
}
```

Generated by [gomarkdoc](<https://github.com/princjef/gomarkdoc>)
